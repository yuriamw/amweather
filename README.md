# Something here should show the weathter.

It's first attempt to write a weather app for android.

# Run in emulator

Either:
```shell
emulator -avd Pixel_8a -accel on -gpu swiftshader_indirect -no-snapshot
```

or
```shell
emulator -avd Pixel_8a -accel on -gpu swiftshader_indirect -no-snapshot -wipe-data
```
